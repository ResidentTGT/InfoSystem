import { ModelLevelNamePipe } from './model-level-name.pipe';

describe('ModelLevelNamePipe', () => {
    it('create an instance', () => {
        const pipe = new ModelLevelNamePipe();
        expect(pipe).toBeTruthy();
    });
});
