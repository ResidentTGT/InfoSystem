import { CeilTwoDigitsPipe } from './ceil-two-digits.pipe';

describe('CeilTwoDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new CeilTwoDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
